README.

按 args 的提示，填相应参数（你可以自行进行参数选择），

``` python main.py --lr 0.001 --lambda_w 0.5 --data_dir /path/to/data --batch_size 2048 --save_dir /path/to/save --hidden_dim 512 --lr_decay_gamma 0.8 --MAX_ITER 100 ```

即可进行模型训练与保存（保存路径默认在 ./results ）

模型参数可见: 
https://drive.google.com/drive/folders/1omFA5BeKNHbeqpkO4zJhDEvSEz5NpRhH?usp=drive_link
